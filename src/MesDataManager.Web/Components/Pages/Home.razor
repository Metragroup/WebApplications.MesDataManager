@page "/"
@inject IStringLocalizer<Strings> L

<PageTitle>@L["App.Title"]</PageTitle>

@*
    L'apertura non e' un cruscotto: chi entra qui deve raggiungere una pagina. Mostra quindi
    lo stesso menu del drawer, dallo stesso componente, perche' su schermo piccolo il drawer
    parte chiuso e senza questa copia non resterebbe nessuna via d'ingresso.
*@

<div class="mes-home">
    <MainNavigation />
</div>
